# Project Instructions & Git Rules

- **Git Workflow**: NEVER create feature branches or pull requests (PRs). Always commit directly and push to the primary branch (`main` / `master`).
- **Commit Messages**: Follow conventional structured prefixes (`feat`, `bugfix`, `refactor`, `chore`, `docs`, etc.) in lowercase with no trailing period.
